name: 🧪 Test Google Play Auth

on:
  workflow_dispatch:

jobs:
  test-auth:
    name: 🛡️ Test JWT Authentication
    runs-on: ubuntu-latest

    steps:
      - name: Create Dummy AAB
        run: |
          mkdir -p build/app/outputs/bundle/release/
          echo "This is a dummy AAB for auth testing" > build/app/outputs/bundle/release/app-release.aab
          echo "${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON_BASE64 }}" | base64 --decode > google-service-account.json

      - name: Attempt Upload (Auth Check)
        uses: r0adkll/upload-google-play@v1.1.2
        with:
          serviceAccountJson: google-service-account.json
          packageName: com.gavra013.gavra_android
          releaseFiles: build/app/outputs/bundle/release/app-release.aab
          track: internal 
          status: draft
